SERVER_PORT=8080
APP_HOST_PORT=8080
SPRING_PROFILES_ACTIVE=local
DOCKERHUB_USERNAME=your-dockerhub-username
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://todayscasting.com,https://www.todayscasting.com

TZ=Asia/Seoul

# LocalDB
# IntelliJ 로컬 실행과 EC2 배포 모두 LOCAL_DB_* 값으로 DB 접속 정보를 주입합니다.
# EC2 배포에서는 LOCAL_DB_URL에 RDS 접속 정보를 넣습니다.
LOCAL_DB_URL=jdbc:mysql://localhost:3306/todays_casting?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
LOCAL_DB_USERNAME=todays
LOCAL_DB_PASSWORD=change-me

# AWS S3
# EC2 배포에서는 IAM Role 권한으로 접근하고, 애플리케이션은 버킷명과 리전만 사용합니다.
AWS_S3_BUCKET_NAME=your-s3-bucket-name
AWS_S3_REGION=your-s3-region

OPENAI_API_KEY=your-openai-api-key-here
JWT_SECRET=replace-with-a-random-secret-at-least-32-bytes-long

# Firebase Admin SDK
FIREBASE_ENABLED=false
FIREBASE_PROJECT_ID=your-firebase-project-id
GOOGLE_APPLICATION_CREDENTIALS=C:/path/to/firebase-service-account.json
